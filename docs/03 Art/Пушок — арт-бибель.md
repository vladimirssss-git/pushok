---
tags: [art, pushok]
статус: черновик
---

# Пушок — арт-бибель

Родитель: [[Пушок — MOC]]

## Технические ограничения

- Пиксель-арт, base tile **32×32**.
- Внутреннее разрешение игры 640×360, масштабирование целочисленное, `pixelArt: true`.
- Спрайт Пушка ~24×28 px.

## Палитра

| Роль | HEX |
| --- | --- |
| Фон | `#1b1b2a` |
| Пушок | `#f2c185` |
| Акцент / UI | `#ffd479` |
| Земля | `#3d5a45` |
| Рыбка | `#8fd3ff` |

## Пайплайн

1. Исходники (`.aseprite`) — в `art-src/`, в git не попадают.
2. Экспорт PNG + JSON атласа → `public/assets/sprites/`.
3. Ключ регистрируется в `src/config/assets.ts`, загрузка — только в `PreloadScene`.

Файлы больше 5 МБ в git не коммитить.

## Связано

[[Пушок — гейм-дизайн]] · [[Пушок — техническая архитектура]]
