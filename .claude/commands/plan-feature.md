---
description: Спланировать игровую фичу до написания кода
---

Задача: $ARGUMENTS

Прочитай `CLAUDE.md`, `AGENTS.md` и релевантные заметки в `docs/01 Design/`.
Затем выдай "Before coding report" из `CLAUDE.md`:

- Ветка (`feature/<short-name>`)
- Задача
- Затронутые модули (`scenes/`, `entities/`, `systems/`, `config/`, assets)
- Файлы, которые изменятся
- Какие константы попадут в `src/config/` и почему
- Какая логика будет чистой функцией в `src/systems/` и как её протестировать
- Риски для game feel и совместимости сейвов
- Тесты и smoke
- Нужно ли одобрение

Код не писать до подтверждения.
