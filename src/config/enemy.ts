/** Параметры врагов. Единственный источник этих чисел. */
export const ENEMY = {
  /** Скорость патрулирования собаки, px/s. */
  dogSpeed: 90,
  /** Скорость погони, когда собака увидела Пушка — быстрее патруля, иначе не «пытается догнать». */
  dogChaseSpeed: 140,
  /** Дальность обнаружения по горизонтали, px. */
  dogDetectionRangeX: 220,
  /** Допуск по высоте от пола, чтобы считать цель «внизу», px. Меньше STEP (45) — уступы не засчитываются. */
  dogDetectionToleranceY: 20,
} as const;
