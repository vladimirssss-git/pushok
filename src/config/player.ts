/**
 * Габариты тела Пушка. Используются и в systems/jumpGeometry.ts — расстановка
 * уступов должна знать реальную ширину тела, а не only физику прыжка «в точке» —
 * и в entities/Pushok.ts, где источник спрайта теперь HQ-разрешения (для чёткости
 * на full HD canvas), но реальный размер тела в игровом мире задаётся тут.
 */
export const PLAYER = {
  width: 24,
  height: 28,
} as const;
