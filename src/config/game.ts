/** Базовые параметры окна и рендера. Менять только вместе с дизайн-заметкой. */
export const GAME = {
  /** Логическая система координат игры — физика и раскладка сцен работают в ней. */
  width: 640,
  height: 360,
  /** Физический canvas рендерится в 1920×1080, а камера сохраняет мир 640×360. */
  renderScale: 3,
  tileSize: 32,
  backgroundColor: '#1b1b2a',
  zoom: 2,
} as const;
