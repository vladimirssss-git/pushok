/** Базовые параметры окна и рендера. Менять только вместе с дизайн-заметкой. */
export const GAME = {
  width: 640,
  height: 360,
  tileSize: 32,
  backgroundColor: '#1b1b2a',
  zoom: 2,
} as const;
